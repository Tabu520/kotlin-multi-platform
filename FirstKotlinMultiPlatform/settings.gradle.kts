pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "FirstKotlinMultiPlatform"
include(":androidApp")
include(":shared")